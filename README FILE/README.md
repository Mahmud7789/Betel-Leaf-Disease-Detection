# For run this Betel Leaf Disease project : 
1. Open vs code terminel.
2. write comand: streamlit run Betel_leaf_web.py


# for seeing the accuracy percentege :   
  with a score of '+ str(np.max(result)*250)
  confidence = round(100 * np.max(result), 2)
  st.info(f"### Confidence: **{confidence}%**")

